export const environment = {
    production: true,    // Set to true for production
    apiUrl: 'https://api.example.com',  // Example API URL for production
    featureFlag: false   // A sample feature flag (disabled in production)
  };
  